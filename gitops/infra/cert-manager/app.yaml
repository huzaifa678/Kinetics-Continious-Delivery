# Catalog entry (ApplicationSet input data — NOT a k8s manifest).
# cert-manager — TLS cert automation. Prerequisite for the HyperPod training
# operator's webhooks. Env values: environments/<env>/values/cert-manager.yaml
name: cert-manager
namespace: cert-manager
chartRepoURL: https://charts.jetstack.io
chartName: cert-manager
chartVersion: "v1.20.2"
