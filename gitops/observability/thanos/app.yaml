---
# Catalog entry (ApplicationSet input data — NOT a k8s manifest).
# Thanos — durable/long-term metrics store on S3, the successor to the AMP
# remote_write target. Topology = RECEIVE (a remote_write endpoint, so it's a
# drop-in for AMP) + Query + Store Gateway + Compactor, all in ns `thanos`.
name: thanos
namespace: thanos
chartRepoURL: https://charts.bitnami.com/bitnami
chartName: thanos
chartVersion: "17.3.1"
