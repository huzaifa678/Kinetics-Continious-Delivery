# Catalog entry (ApplicationSet input data — NOT a k8s manifest).
# NVIDIA DCGM exporter — GPU metrics. No env-specific values.
name: dcgm-exporter
namespace: monitoring
chartRepoURL: https://nvidia.github.io/dcgm-exporter/helm-charts
chartName: dcgm-exporter
chartVersion: "3.6.1"
