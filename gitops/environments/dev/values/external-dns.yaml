---
# external-dns — dev cluster. Pod-Identity SA (external-dns/kube-system).
provider:
  name: aws
policy: upsert-only
registry: txt
txtOwnerId: kinetics-pipeline-dev
serviceAccount:
  create: true
  name: external-dns
env:
  - name: AWS_REGION
    value: us-east-1
# domainFilters:
#   - inference.dev.example.com
