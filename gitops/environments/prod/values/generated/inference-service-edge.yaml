# =============================================================================
# GENERATED FILE -- DO NOT EDIT.
#
# Rendered from the GitOps contract published by terraform/infra/gitops-contract.tf
# (SSM parameter /kinetics-pipeline/prod/gitops-contract) by
# scripts/render-generated-values.py.
#
# Hand edits are overwritten by the next render-and-PR run. Change the template
# at gitops/environments/_generated/inference-service-edge.tpl.yaml instead, or change the Terraform output that feeds it.
#
# Environment: prod
# =============================================================================
# gitops-render-envs: prod
---
# PROD-ONLY public edge for the inference API: internet-facing ALB + WAF, docs
# off, CORS locked to the SPA origin, Cognito JWT auth. All values come from the
# GitOps contract (WAF ARN + Cognito client ids regenerate with terraform, so
# they must not be hand-copied). Dev keeps the internal posture from its base
# values file and this overlay is simply absent (ignoreMissingValueFiles).
ingress:
  enabled: true
  scheme: internet-facing
  host: "api.freeeasycrypto.com"
  wafAclArn: "arn:aws:wafv2:us-east-1:533267178572:regional/webacl/kinetics-pipeline-prod-api-waf/d637a5c5-dca8-4259-90c8-8e3a8296cc2b"
app:
  enableDocs: "false"
  corsAllowOrigins: "https://d2ktbizj92gozf.cloudfront.net"
  cognito:
    issuer: "https://cognito-idp.us-east-1.amazonaws.com/us-east-1_506mwBPAH"
    audience: "2il3kofoejnoe3ape0jadpkfa6,24hsn9pa1060p6rv685hqo1nur"
